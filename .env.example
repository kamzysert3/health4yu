STRIPE_SECRET_KEY=sk_test_your_key_here
PORT=4000

SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_user
SMTP_PASS=your_pass
SMTP_SECURE=false

MAIL_DELETE_UPLOADS=true

CONTACT_FEE_CENTS=1000
CONTACT_FEE_CURRENCY=eur

CONTACT_REQUIRE_PAYMENT=true

# Production notes
# - For live operation use your Stripe *live* secret key (starts with sk_live_...) and production SMTP credentials.
# - Ensure MAIL_DELETE_UPLOADS is true to avoid filling up disk space with uploaded files.
# - Set CONTACT_REQUIRE_PAYMENT to true to require payment before processing contact form submissions.
# - Adjust CONTACT_FEE_CENTS and CONTACT_FEE_CURRENCY as needed for your consultation fee.
