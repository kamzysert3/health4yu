# .env.example — Example environment variables for Health4Yu
# -----------------------------------------------------------------------------
# Copy this file to `.env` and populate real secret values for your environment.
# Never commit real secrets to source control. Use a secret manager in production.
#
# Format: KEY=value
# Lines that begin with `#` are comments and ignored by the application.

# Stripe
# STRIPE_SECRET_KEY is the Stripe secret API key used to create Checkout sessions.
# Use a test key (starts with `sk_test_`) while developing. Use a live key
# (starts with `sk_live_`) in production.
STRIPE_SECRET_KEY=sk_test_your_key_here

# Server port the Express app will listen on (default in docs: 4000).
PORT=4000

# SMTP configuration for sending emails (Nodemailer)
# - SMTP_HOST: hostname of your SMTP provider (e.g. smtp.gmail.com)
# - SMTP_PORT: port (commonly 587 for STARTTLS or 465 for implicit TLS)
# - SMTP_USER/SMTP_PASS: credentials for authentication
# - SMTP_SECURE: true for implicit TLS (465), false for STARTTLS (587)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your_user
SMTP_PASS=your_pass
SMTP_SECURE=false

# MAIL_DELETE_UPLOADS controls whether uploaded attachments are deleted
# after a successful send. Default true (recommended) to avoid storing
# sensitive medical documents on disk in production. Set to `false` only
# for debugging purposes and ensure you have a secure retention policy.
MAIL_DELETE_UPLOADS=true

# CONTACT_FEE_CENTS — the fee charged for a contact submission, expressed
# in the smallest currency unit (cents). Default 1000 means €10.00 when
# CONTACT_FEE_CURRENCY=eur.
CONTACT_FEE_CENTS=1000

# CONTACT_FEE_CURRENCY — three-letter currency code for the contact fee.
# Common examples: eur, usd, gbp (lowercase is accepted). Default: eur.
CONTACT_FEE_CURRENCY=eur

# CONTACT_REQUIRE_PAYMENT — set to `true` to require payment before the
# server sends contact messages. Set `false` for local development/testing
# to skip Stripe and send immediately.
CONTACT_REQUIRE_PAYMENT=true

